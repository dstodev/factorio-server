[project]
name = 'manage'
version = '1.0.0'
requires-python = ">=3.10"
dependencies = ['docker']

[project.scripts]
manage = 'manage.cli:main'

[build-system]
requires = ['setuptools']
build-backend = 'setuptools.build_meta'
